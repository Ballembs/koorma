# Google AI — Gemini API key from Google AI Studio
GEMINI_API_KEY=your-gemini-api-key-here

# Google Cloud — Service account for TTS and STT
# Download from Google Cloud Console → IAM → Service Accounts → Keys
# Place the JSON file in project root (it's gitignored)
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
